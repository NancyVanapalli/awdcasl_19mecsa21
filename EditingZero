import java.util.*;
class EditingZero
{
 public static void main(String args[])
  {
     int n,count=0;
     Scanner sc=new Scanner(System.in);
     n=sc.nextInt();
     int a[]=new int[n];
     for(int i=0;i<n;i++)
      a[i]=sc.nextInt();
    for(int i=0;i<n;i++)
     {
        if(a[i]!=0)
         {
            count++;
            System.out.print(a[i]+" ");
          }
     
     }
 int rem=n-count;
 for(int i=0;i<rem;i++)
   System.out.print("0"+" ");
 }
}
